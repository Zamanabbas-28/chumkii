# Supabase (anon / public key only — never commit service_role)
# Optional locally: defaults are in src/config/supabasePublic.js
# For Vercel: Project Settings → Environment Variables → add both vars, then redeploy
VITE_SUPABASE_URL=https://lsbdjmlvbnqznsebooef.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
